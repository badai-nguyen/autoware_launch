# This file is managed by sync-params workflow.
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/tier4/autoware_universe/blob/4e2342b7ccc9d5b1006ad5e2edec6768f39dd7d3/perception/autoware_euclidean_cluster/config/euclidean_cluster.param.yaml

/**:
  ros__parameters:
    max_cluster_size: 1000
    min_points_per_cluster: 10
    tolerance_m: 0.7
    use_height: false

# ###### ORIGINAL (DO NOT EDIT) ######
# /**:
#   ros__parameters:
#     max_cluster_size: 1000
#     min_points_per_cluster: 10
#     tolerance_m: 0.7
#     use_height: false
