# This file is managed by sync-params workflow.
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/tier4/autoware_universe/blob/c3a838767a41e432baf0cd77aeee542cfb276ab3/perception/autoware_object_sorter/config/tracked_object_sorter.param.yaml

/**:
  ros__parameters:
    range_calc_frame_id: "base_link"
    range_calc_offset:
      x: 0.0 # [m]
      y: 0.0 # [m]

    range_thresholding_mode: "distance" # [distance, area]

    sort_target:
      UNKNOWN:
        publish: true
        min_velocity_threshold: 3.0 # {OVERRIDE} [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 300.0 # {OVERRIDE} [m]
          min_distance: 70.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 100.0 # [m]
          min_x: -100.0 # [m]
          max_y: 100.0 # [m]
          min_y: -100.0 # [m]

      CAR:
        publish: true
        min_velocity_threshold: 3.0 # {OVERRIDE} [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 300.0 # {OVERRIDE} [m]
          min_distance: 70.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 100.0 # [m]
          min_x: -100.0 # [m]
          max_y: 30.0 # [m]
          min_y: -30.0 # [m]

      TRUCK:
        publish: true
        min_velocity_threshold: 3.0 # {OVERRIDE} [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 300.0 # {OVERRIDE} [m]
          min_distance: 70.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 100.0 # [m]
          min_x: -100.0 # [m]
          max_y: 100.0 # [m]
          min_y: -100.0 # [m]

      BUS:
        publish: true
        min_velocity_threshold: 3.0 # {OVERRIDE} [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 300.0 # {OVERRIDE} [m]
          min_distance: 70.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 100.0 # [m]
          min_x: -100.0 # [m]
          max_y: 100.0 # [m]
          min_y: -100.0 # [m]

      TRAILER:
        publish: true
        min_velocity_threshold: 3.0 # {OVERRIDE} [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 300.0 # {OVERRIDE} [m]
          min_distance: 70.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 100.0 # [m]
          min_x: -100.0 # [m]
          max_y: 100.0 # [m]
          min_y: -100.0 # [m]

      MOTORCYCLE:
        publish: true
        min_velocity_threshold: 3.0 # {OVERRIDE} [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 300.0 # {OVERRIDE} [m]
          min_distance: 70.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 100.0 # [m]
          min_x: -100.0 # [m]
          max_y: 100.0 # [m]
          min_y: -100.0 # [m]

      BICYCLE:
        publish: true
        min_velocity_threshold: 3.0 # {OVERRIDE} [m/s]
        range_threshold:
         # when range_thresholding_mode = distance
          max_distance: 300.0 # {OVERRIDE} [m]
          min_distance: 70.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 100.0 # [m]
          min_x: -100.0 # [m]
          max_y: 100.0 # [m]
          min_y: -100.0 # [m]

      PEDESTRIAN:
        publish: true
        min_velocity_threshold: 3.0 # {OVERRIDE} [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 300.0 # {OVERRIDE} [m]
          min_distance: 70.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 100.0 # {OVERRIDE} [m]
          min_x: -100.0 # {OVERRIDE} [m]
          max_y: 100.0 # {OVERRIDE} [m]
          min_y: -100.0 # {OVERRIDE} [m]

      ANIMAL:
        publish: true
        min_velocity_threshold: 0.0 # [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 100.0 # [m]
          min_distance: 30.0 # [m]
          # when range_thresholding_mode = area
          max_x: 100.0 # [m]
          min_x: -100.0 # [m]
          max_y: 100.0 # [m]
          min_y: -100.0 # [m]

      HAZARD:
        publish: true
        min_velocity_threshold: 0.0 # [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 100.0 # [m]
          min_distance: 30.0 # [m]
          # when range_thresholding_mode = area
          max_x: 100.0 # [m]
          min_x: -100.0 # [m]
          max_y: 100.0 # [m]
          min_y: -100.0 # [m]

# ###### ORIGINAL (DO NOT EDIT) ######
# /**:
#   ros__parameters:
#     range_calc_frame_id: "base_link"
#     range_calc_offset:
#       x: 0.0 # [m]
#       y: 0.0 # [m]
#
#     range_thresholding_mode: "distance" # [distance, area]
#
#     sort_target:
#       UNKNOWN:
#         publish: true
#         min_velocity_threshold: 0.0 # [m/s]
#         range_threshold:
#           # when range_thresholding_mode = distance
#           max_distance: 100.0 # [m]
#           min_distance: 30.0 # [m]
#           # when range_thresholding_mode = area
#           max_x: 100.0 # [m]
#           min_x: -100.0 # [m]
#           max_y: 100.0 # [m]
#           min_y: -100.0 # [m]
#
#       CAR:
#         publish: true
#         min_velocity_threshold: 0.0 # [m/s]
#         range_threshold:
#           # when range_thresholding_mode = distance
#           max_distance: 100.0 # [m]
#           min_distance: 30.0 # [m]
#           # when range_thresholding_mode = area
#           max_x: 100.0 # [m]
#           min_x: -100.0 # [m]
#           max_y: 30.0 # [m]
#           min_y: -30.0 # [m]
#
#       TRUCK:
#         publish: true
#         min_velocity_threshold: 0.0 # [m/s]
#         range_threshold:
#           # when range_thresholding_mode = distance
#           max_distance: 100.0 # [m]
#           min_distance: 30.0 # [m]
#           # when range_thresholding_mode = area
#           max_x: 100.0 # [m]
#           min_x: -100.0 # [m]
#           max_y: 100.0 # [m]
#           min_y: -100.0 # [m]
#
#       BUS:
#         publish: true
#         min_velocity_threshold: 0.0 # [m/s]
#         range_threshold:
#           # when range_thresholding_mode = distance
#           max_distance: 100.0 # [m]
#           min_distance: 30.0 # [m]
#           # when range_thresholding_mode = area
#           max_x: 100.0 # [m]
#           min_x: -100.0 # [m]
#           max_y: 100.0 # [m]
#           min_y: -100.0 # [m]
#
#       TRAILER:
#         publish: true
#         min_velocity_threshold: 0.0 # [m/s]
#         range_threshold:
#           # when range_thresholding_mode = distance
#           max_distance: 100.0 # [m]
#           min_distance: 30.0 # [m]
#           # when range_thresholding_mode = area
#           max_x: 100.0 # [m]
#           min_x: -100.0 # [m]
#           max_y: 100.0 # [m]
#           min_y: -100.0 # [m]
#
#       MOTORCYCLE:
#         publish: true
#         min_velocity_threshold: 0.0 # [m/s]
#         range_threshold:
#           # when range_thresholding_mode = distance
#           max_distance: 100.0 # [m]
#           min_distance: 30.0 # [m]
#           # when range_thresholding_mode = area
#           max_x: 100.0 # [m]
#           min_x: -100.0 # [m]
#           max_y: 100.0 # [m]
#           min_y: -100.0 # [m]
#
#       BICYCLE:
#         publish: true
#         min_velocity_threshold: 0.0 # [m/s]
#         range_threshold:
#          # when range_thresholding_mode = distance
#           max_distance: 100.0 # [m]
#           min_distance: 30.0 # [m]
#           # when range_thresholding_mode = area
#           max_x: 100.0 # [m]
#           min_x: -100.0 # [m]
#           max_y: 100.0 # [m]
#           min_y: -100.0 # [m]
#
#       PEDESTRIAN:
#         publish: true
#         min_velocity_threshold: 0.0 # [m/s]
#         range_threshold:
#           # when range_thresholding_mode = distance
#           max_distance: 100.0 # [m]
#           min_distance: 30.0 # [m]
#           # when range_thresholding_mode = area
#           max_x: 100.0 # [m]
#           min_x: -100.0 # [m]
#           max_y: 100.0 # [m]
#           min_y: -100.0 # [m]
#
#       ANIMAL:
#         publish: true
#         min_velocity_threshold: 0.0 # [m/s]
#         range_threshold:
#           # when range_thresholding_mode = distance
#           max_distance: 100.0 # [m]
#           min_distance: 30.0 # [m]
#           # when range_thresholding_mode = area
#           max_x: 100.0 # [m]
#           min_x: -100.0 # [m]
#           max_y: 100.0 # [m]
#           min_y: -100.0 # [m]
#
#       HAZARD:
#         publish: true
#         min_velocity_threshold: 0.0 # [m/s]
#         range_threshold:
#           # when range_thresholding_mode = distance
#           max_distance: 100.0 # [m]
#           min_distance: 30.0 # [m]
#           # when range_thresholding_mode = area
#           max_x: 100.0 # [m]
#           min_x: -100.0 # [m]
#           max_y: 100.0 # [m]
#           min_y: -100.0 # [m]
